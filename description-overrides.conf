# Custom description overrides for README generation
# Format: name=description
# These take priority over auto-detected descriptions from SKILL.md / plugin.json

swarm=Spin up autonomous agent teams with bypassed permissions
prism=Write and compile LaTeX in OpenAI's Prism editor via browser automation
pdf=Create, merge, split, fill, and manipulate PDF files

# MCP servers (prefix with mcp-)
mcp-voicemode=Voice conversation with STT/TTS via ElevenLabs + Kokoro + OpenAI
mcp-memory=Persistent knowledge graph across conversations
mcp-claude-in-chrome=Browser automation via Chrome extension (screenshots, clicks, navigation, forms)
